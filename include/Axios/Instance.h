#pragma once

namespace ax
{
	namespace Instance
	{
		// Starts the game loop
		void start();

        // Initialize all modules
        void init();
		// Deinitializes all modules
		void deinit();
	}
}